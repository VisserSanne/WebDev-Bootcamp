clear()
// This clears the console. This is a method, so there is some code that we can easily use this

// alert    This pops up a message to the user of the site
alert("Hi there!")

// prompt   It lets us get input from a user (so the user will get a pop-up message)
prompt("What is your name?")
var userName = prompt("What is your name?");    // Now you have stored the input of the user

//console.log   This wil print a something to the Java console. So if that isn't opened you can't see it
console.log("Hello from the console!")
