// Often we want to write functions that take inputs. This is how you do that

function square(num) {
    console.log(num * num);
}

// Now when you call, you should put a number between the brackets
square(10);     // prints 100
square(3);      // prints 9
square(4);      // prints 16
